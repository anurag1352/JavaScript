let cars = ["audi","bmw","kia","xuv"];
cars.push("toyota","nano"); //push add element at the end.
cars.pop(); //pop delete element from the end.
cars.unshift("nano","tuk-tuk") // unshift add element starting of the array.
cars.shift(); //shift delete element from the ofstarting  array
console.log(cars);

//eg.
let follower = ["a","b","c","d"];
let block = follower.shift();
console.log(follower);
console.log(block);